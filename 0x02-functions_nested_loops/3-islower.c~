# include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int _islower(int c);
int main(void)
{
int c='a';
_islower(c);
}


int _islower(int c)
{
if (islower(c))
{
return (1);
}else
{
return (0);
}

}
