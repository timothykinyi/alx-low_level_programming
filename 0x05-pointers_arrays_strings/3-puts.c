# include <stdio.h>
# include "main.h"
11;rgb:0000/0000/0000/**
 * _puts - prints a line to stdout
 * @str: string to be printed
 * return : nothing
 */
void _puts(char *str)
{
while (*str != '\0')
{
puts(str);
s++;
}
}
