# include <stdio.h>

int main(void)
{
for (char character = "a" ; character <= "z" ; character++)
{
putchar(character);
}
for (char character = "A" ; character <= "Z" ; character++)
{
putchar(character);
}
putchar("\n");
return (0);
}
